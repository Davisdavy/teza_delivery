/**
 * Inbound web adapter for the auth module.
 *
 * <p>REST controllers (login, token refresh, registration) and the
 * request/response DTOs that form this module's public HTTP contract. Maps HTTP
 * to application-service calls and holds no business rules.
 */
package com.wafula.teza.auth.api;
