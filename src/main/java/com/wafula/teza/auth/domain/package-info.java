/**
 * Core domain model for the auth module.
 *
 * <p>Entities, value objects, domain events, and repository interfaces (ports)
 * for users, roles, and credentials. Pure business model — no framework or
 * persistence concerns leak in here.
 */
package com.wafula.teza.auth.domain;
