/**
 * Application / use-case layer for the auth module.
 *
 * <p>Orchestrates the domain model, defines transaction boundaries, and
 * coordinates with other modules through events. This is the only layer other
 * modules are allowed to depend on.
 */
package com.wafula.teza.auth.application;
