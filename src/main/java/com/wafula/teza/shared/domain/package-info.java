/**
 * Reusable domain building blocks shared by all modules.
 *
 * <p>Examples: base entity / aggregate-root types, auditing fields, common
 * value objects, and the marker interface(s) for domain events. Contains only
 * generic, business-agnostic abstractions.
 */
package com.wafula.teza.shared.domain;
