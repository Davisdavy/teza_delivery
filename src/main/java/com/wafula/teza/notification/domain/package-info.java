/**
 * Core domain model for the notification module.
 *
 * <p>Notification entities/value objects, channel and template abstractions,
 * recipient preferences, and the sender repository ports. Pure business model
 * with no framework or persistence concerns.
 */
package com.wafula.teza.notification.domain;
