/**
 * Application / use-case layer for the notification module.
 *
 * <p>Event listeners that turn domain events into notifications, resolve
 * recipient preferences, select a channel, and dispatch through the domain
 * ports. The only layer other modules may depend on.
 */
package com.wafula.teza.notification.application;
