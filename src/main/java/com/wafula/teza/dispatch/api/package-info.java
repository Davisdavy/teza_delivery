/**
 * Inbound web adapter for the dispatch module.
 *
 * <p>REST controllers and DTOs for admin/ops to inspect or override
 * assignments. Much of dispatch is event-driven, so this layer is intentionally
 * thin. No business rules live here.
 */
package com.wafula.teza.dispatch.api;
