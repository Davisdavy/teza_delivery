/**
 * Inbound web adapter for the user module.
 *
 * <p>Will hold REST controllers and DTOs for profile management (e.g. view/update
 * own profile, admin user administration). Empty for now — no user-facing endpoints
 * exist yet.
 */
package com.wafula.teza.user.api;
