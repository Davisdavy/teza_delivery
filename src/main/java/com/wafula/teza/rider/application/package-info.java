/**
 * Application / use-case layer for the rider module.
 *
 * <p>Orchestrates the rider domain, defines transaction boundaries, and
 * publishes/handles cross-module events. The only layer other modules may
 * depend on.
 */
package com.wafula.teza.rider.application;
