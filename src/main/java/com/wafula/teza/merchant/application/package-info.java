/**
 * Application / use-case layer for the merchant module.
 *
 * <p>Orchestrates the merchant domain, defines transaction boundaries, and
 * publishes/handles cross-module events. The only layer other modules may
 * depend on.
 */
package com.wafula.teza.merchant.application;
