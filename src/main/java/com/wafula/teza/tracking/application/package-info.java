/**
 * Application / use-case layer for the tracking module.
 *
 * <p>Processes incoming location updates, maintains current tracking state, and
 * publishes/handles cross-module events. The only layer other modules may
 * depend on.
 */
package com.wafula.teza.tracking.application;
