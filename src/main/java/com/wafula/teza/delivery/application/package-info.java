/**
 * Application / use-case layer for the delivery module.
 *
 * <p>Orchestrates the delivery domain, defines transaction boundaries, and
 * publishes/handles cross-module events. The only layer other modules may
 * depend on.
 */
package com.wafula.teza.delivery.application;
